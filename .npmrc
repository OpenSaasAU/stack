# Don't fail on peer dependency warnings
strict-peer-dependencies=false

# Use prebuild binaries when available to avoid compilation issues
# This helps with better-sqlite3 and other native modules
prefer-offline=false
