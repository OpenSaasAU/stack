# Preset review screenshots (issue #707)

Light + dark captures of the re-curated `modern` / `classic` / `neon` presets,
taken from the `examples/starter` admin (`/admin/post`) with only the config's
`ui.theme.preset` changed between runs (brand overrides removed).

These are attached for the HITL design sign-off on the PR. They can be removed
before merge, or promoted into the preset gallery when the theming docs land
(the separate docs slice of #704).
