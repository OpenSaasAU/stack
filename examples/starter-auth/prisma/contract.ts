// ⚠️  GENERATED FILE - DO NOT EDIT
// Generated by 'opensaas generate' from opensaas.config.ts.

import { defineContract } from '@prisma/orm-postgres/contract-builder'
import type { ScalarFieldBuilder } from '@prisma/orm-postgres/contract-builder'
import { timestamptzStringColumn } from '@prisma/orm-postgres/adapter/column-types'

type ModelToken = {
  readonly stageOne: {
    readonly modelName?: string
    readonly fields: Record<string, ScalarFieldBuilder>
  }
}

export const contract = defineContract(
  { extensions: {} },
  ({ field, model, rel }) => {
    const models: Record<string, ModelToken> = {}

    const model_Post = (models.Post = model('Post', {
      fields: {
        id: field.id.uuidv7Native(),
        title: field.text(),
        slug: field.text().unique(),
        content: field.text().optional(),
        internalNotes: field.text().optional(),
        status: field.text().default('draft'),
        publishedAt: field.column(timestamptzStringColumn).optional(),
        viewCount: field.int().optional().default(0),
        authorId: field.uuidNative().optional().column('author'),
      },
      relations: {
        author: rel.belongsTo(() => models.User, { from: 'authorId', to: 'id' }),
      },
    }))

    const model_Note = (models.Note = model('Note', {
      fields: {
        id: field.id.uuidv7Native(),
        body: field.text(),
        ownerId: field.uuidNative().optional().column('owner'),
      },
      relations: {
        owner: rel.belongsTo(() => models.User, { from: 'ownerId', to: 'id' }),
      },
    }))

    const model_User = (models.User = model('User', {
      fields: {
        id: field.id.uuidv7Native(),
        name: field.text(),
        email: field.text().unique(),
        emailVerified: field.boolean().default(false),
        image: field.text().optional(),
        createdAt: field.temporal.createdAtString(),
        updatedAt: field.temporal.updatedAtString(),
      },
      relations: {
        sessions: rel.hasMany(() => models.Session, { by: 'userId' }),
        accounts: rel.hasMany(() => models.Account, { by: 'userId' }),
        posts: rel.hasMany(() => models.Post, { by: 'authorId' }),
        notes: rel.hasMany(() => models.Note, { by: 'ownerId' }),
      },
    }))

    const model_Session = (models.Session = model('Session', {
      fields: {
        id: field.id.uuidv7Native(),
        token: field.text().unique(),
        expiresAt: field.column(timestamptzStringColumn),
        ipAddress: field.text().optional(),
        userAgent: field.text().optional(),
        userId: field.uuidNative(),
        createdAt: field.temporal.createdAtString(),
        updatedAt: field.temporal.updatedAtString(),
      },
      relations: {
        user: rel.belongsTo(() => models.User, { from: 'userId', to: 'id' }),
      },
    }))

    const model_Account = (models.Account = model('Account', {
      fields: {
        id: field.id.uuidv7Native(),
        accountId: field.text(),
        providerId: field.text(),
        issuer: field.text(),
        userId: field.uuidNative(),
        accessToken: field.text().optional(),
        refreshToken: field.text().optional(),
        accessTokenExpiresAt: field.column(timestamptzStringColumn).optional(),
        refreshTokenExpiresAt: field.column(timestamptzStringColumn).optional(),
        scope: field.text().optional(),
        idToken: field.text().optional(),
        password: field.text().optional(),
        createdAt: field.temporal.createdAtString(),
        updatedAt: field.temporal.updatedAtString(),
      },
      relations: {
        user: rel.belongsTo(() => models.User, { from: 'userId', to: 'id' }),
      },
    }))

    const model_Verification = (models.Verification = model('Verification', {
      fields: {
        id: field.id.uuidv7Native(),
        identifier: field.text(),
        value: field.text(),
        expiresAt: field.column(timestamptzStringColumn),
        createdAt: field.temporal.createdAtString(),
        updatedAt: field.temporal.updatedAtString(),
      },
      relations: {},
    }))

    return {
      models: {
        Post: model_Post.attributes(() => ({
          uniques: [],
        })).sql(({ cols, constraints }) => ({
          indexes: [
            constraints.index([cols.authorId]),
          ],
          foreignKeys: [
            constraints.foreignKey(cols.authorId, model_User.refs.id, { index: false }),
          ],
        })),
        Note: model_Note.attributes(() => ({
          uniques: [],
        })).sql(({ cols, constraints }) => ({
          indexes: [
            constraints.index([cols.ownerId]),
          ],
          foreignKeys: [
            constraints.foreignKey(cols.ownerId, model_User.refs.id, { index: false }),
          ],
        })),
        User: model_User.attributes(() => ({
          uniques: [],
        })).sql(() => ({
          indexes: [],
          foreignKeys: [],
        })),
        Session: model_Session.attributes(() => ({
          uniques: [],
        })).sql(({ cols, constraints }) => ({
          indexes: [
            constraints.index([cols.userId]),
          ],
          foreignKeys: [
            constraints.foreignKey(cols.userId, model_User.refs.id, { index: false, onDelete: 'cascade' }),
          ],
        })),
        Account: model_Account.attributes(() => ({
          uniques: [],
        })).sql(({ cols, constraints }) => ({
          indexes: [
            constraints.index([cols.userId]),
          ],
          foreignKeys: [
            constraints.foreignKey(cols.userId, model_User.refs.id, { index: false, onDelete: 'cascade' }),
          ],
        })),
        Verification: model_Verification.attributes(() => ({
          uniques: [],
        })).sql(({ cols, constraints }) => ({
          indexes: [
            constraints.index([cols.identifier]),
          ],
          foreignKeys: [],
        })),
      },
    }
  },
)
