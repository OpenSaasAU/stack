{
  ".changeset/swift-pandas-listen.md:14": {
    "excuses": ["provider"],
    "reason": "a bare name the prose supplies"
  },

  "docs/content/how-to/rag-advanced.md:73": {
    "whole": "the body of an object literal: `runtime:` and `afterTransaction:` are bare properties, not statements, and the hook's parameters are typed by the list it is injected into"
  },
  "docs/content/how-to/rag-advanced.md:239": {
    "excuses": ["TS2307 'cohere-ai'"],
    "reason": "a third-party SDK this repo does not install"
  },
  "docs/content/how-to/rag-advanced.md:299": {
    "excuses": ["TS2307 '@/lib/providers/cohere'"],
    "reason": "imports the file the reader wrote in the block above"
  },
  "docs/content/how-to/rag-advanced.md:331": {
    "excuses": ["TS2307 '@huggingface/inference'"],
    "reason": "a third-party SDK this repo does not install"
  },
  "docs/content/how-to/rag-advanced.md:542": {
    "whole": "a bare `DocumentChunk:` object-literal property — one entry of a `lists:` object — not a statement"
  },
  "docs/content/how-to/rag-advanced.md:710": {
    "excuses": ["TS2451 'matches'"],
    "reason": "a bad/good pair shares one fence and both snippets bind `matches`"
  },
  "docs/content/how-to/rag-advanced.md:847": {
    "excuses": ["TS2307 '@huggingface/inference'"],
    "reason": "a third-party SDK this repo does not install"
  },
  "docs/content/how-to/rag-advanced.md:902": {
    "whole": "a bare `lists:` object-literal property, not a statement"
  },

  "docs/content/reference/rag.md:169": {
    "whole": "a bare `fields:` object-literal property, not a statement"
  },
  "docs/content/reference/rag.md:266": {
    "whole": "a bare `fields:` object-literal property, not a statement"
  },

  "examples/rag-ollama-demo/README.md:151": {
    "excuses": ["TS2345 'lists'"],
    "reason": "a `config()` whose `lists` the prose elides"
  },
  "examples/rag-ollama-demo/README.md:176": {
    "whole": "a bare `fields:` object-literal property, not a statement"
  },
  "examples/rag-ollama-demo/README.md:198": {
    "whole": "a bare `fields:` object-literal property, not a statement"
  },
  "examples/rag-ollama-demo/README.md:442": {
    "excuses": ["provider", "text1", "text2", "text3"],
    "reason": "bare names the prose supplies"
  },

  "packages/rag/CLAUDE.md:143": {
    "whole": "a bare `fields:` object-literal property, not a statement"
  },
  "packages/rag/CLAUDE.md:177": {
    "whole": "a bare `fields:` object-literal property, not a statement"
  },
  "packages/rag/CLAUDE.md:516": {
    "excuses": ["provider"],
    "reason": "a bare name the prose supplies"
  },
  "packages/rag/CLAUDE.md:531": {
    "excuses": ["query"],
    "reason": "the caller's search string, a bare name the prose supplies"
  },
  "packages/rag/CLAUDE.md:544": {
    "excuses": ["id"],
    "reason": "a bare name the prose supplies"
  },
  "packages/rag/CLAUDE.md:560": {
    "excuses": ["TS2307 './openai.js'"],
    "reason": "a relative import from inside the package's own test tree"
  },

  "packages/rag/README.md:266": {
    "whole": "a bare `fields:` object-literal property, not a statement"
  },
  "packages/rag/README.md:379": {
    "excuses": ["TS2451 'chunks'"],
    "reason": "three alternative snippets share one fence and each binds `chunks`"
  }
}
