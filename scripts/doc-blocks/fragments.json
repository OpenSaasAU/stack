{
  ".changeset/swift-pandas-listen.md:14": "`provider` is a bare name the prose supplies.",

  "docs/content/how-to/rag-advanced.md:66": "the body of an object literal: `runtime:` and `afterTransaction:` are bare properties, not statements. `WRITE_EMBEDDING`, `writePluginOwnedField`, `embeddingWriter`, `sourceField`, `fieldName`, `listName` and `provider` are bindings the plugin holds, and the hook's parameters are typed by the list it is injected into.",
  "docs/content/how-to/rag-advanced.md:234": "imports `cohere-ai`, a third-party SDK this repo does not install.",
  "docs/content/how-to/rag-advanced.md:296": "imports `@/lib/providers/cohere`, the file the reader wrote in the block above, and passes a `config()` whose `lists` the prose elides.",
  "docs/content/how-to/rag-advanced.md:317": "imports `@huggingface/inference`, a third-party SDK this repo does not install.",
  "docs/content/how-to/rag-advanced.md:523": "a bare `DocumentChunk:` object-literal property — one entry of a `lists:` object — not a statement.",
  "docs/content/how-to/rag-advanced.md:687": "two alternative snippets share one fence and both bind `matches`.",
  "docs/content/how-to/rag-advanced.md:825": "imports `@huggingface/inference`, a third-party SDK this repo does not install.",
  "docs/content/how-to/rag-advanced.md:880": "a bare `lists:` object-literal property, not a statement.",
  "docs/content/how-to/rag-advanced.md:910": "`context.db.Article.nearest()` on the un-parameterised `StackContext` yields `NearestMatch<OrmRow>`, and `OrmRow` carries no `id`, so it does not fit the block's own `NearestMatch<{ id: string }>[]`. In a reader's project the row comes from the generated TypeInfo and does.",

  "docs/content/reference/rag.md:169": "a bare `fields:` object-literal property, not a statement.",
  "docs/content/reference/rag.md:266": "a bare `fields:` object-literal property, not a statement.",
  "docs/content/reference/rag.md:396": "`openaiEmbeddings({/* ... */})` and `ollamaEmbeddings({/* ... */})` are written as elisions, so the required `apiKey` and `dimensions` are absent by design.",
  "docs/content/reference/rag.md:669": "three alternative snippets share one fence and each binds `chunks`.",

  "examples/rag-ollama-demo/README.md:151": "a `config()` whose `lists` the prose elides.",
  "examples/rag-ollama-demo/README.md:176": "a bare `fields:` object-literal property, not a statement.",
  "examples/rag-ollama-demo/README.md:198": "a bare `fields:` object-literal property, not a statement.",
  "examples/rag-ollama-demo/README.md:442": "`provider`, `text1`, `text2` and `text3` are bare names the prose supplies.",

  "packages/rag/CLAUDE.md:143": "a bare `fields:` object-literal property, not a statement.",
  "packages/rag/CLAUDE.md:177": "a bare `fields:` object-literal property, not a statement.",
  "packages/rag/CLAUDE.md:516": "`provider` is a bare name the prose supplies.",
  "packages/rag/CLAUDE.md:531": "`query` — the caller's search string — is a bare name the prose supplies.",
  "packages/rag/CLAUDE.md:546": "`id` is a bare name the prose supplies, and `article.contentEmbedding` is `unknown` against the un-parameterised `StackContext`, which the `stored ?` guard narrows to `{}` — so `stored.vector` does not resolve.",
  "packages/rag/CLAUDE.md:562": "imports `./openai.js` by relative path from inside the package's own test tree.",

  "packages/rag/README.md:136": "a `config()` whose `lists` the prose elides.",
  "packages/rag/README.md:255": "a bare `fields:` object-literal property, not a statement.",
  "packages/rag/README.md:368": "three alternative snippets share one fence and each binds `chunks`."
}
